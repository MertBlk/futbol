{
  "name": "futbol",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:web": "tsc && cp index.html styles.css dist/",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "serve": "npm run build:web && npx serve dist -l 3000",
    "demo": "npm run build:web && npx serve dist -l 3000"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "serve": "^14.2.5",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}
